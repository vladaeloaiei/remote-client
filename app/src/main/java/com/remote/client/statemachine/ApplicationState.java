package com.remote.client.statemachine;

/**
 * Enum for application state
 */
public enum ApplicationState
{
    DISCONNECTED,
    CONNECTED,
    REMOTE_DESKTOP,
    TOUCHPAD,
    KEYBOARD,
    FUNCTIONAL_KEYS,
    IMAGE_TRANSFER
}
